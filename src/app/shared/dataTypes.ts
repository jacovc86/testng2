export enum UserType {
    SHOPPER,
    VENDOR
}
export enum PostType {
    OFFER,
    SHOPPING_GROUP
}
export enum DeliveryOption {
    ARRANGED
}
export enum ReasonType {
    FOLLOWED_COMMITTED,
    FOLLOWED_PURCHASED
}

export enum AuthType {
    INTERNAL,
    FACEBOOK
}

export enum WebsiteSource {
    SHOPIZ,
    CALCALIST,
    MAMAZONE
}

export enum NotificationType {
    COMMENT,
    OFFER_CREATED
}

export enum NotificationRelationType {
    MEMBER
}
export enum DtoType {
    USER,
    OFFER,
    SHOPPING_GROUP,
    COMMENT,
    PRODUCT,
    CATEGORY,
    BRAND
}
